const { formatGuestList } = require("../kata/section-1");
const { expect } = require("chai");

describe("formatGuestList", () => {
  it("", () => {
    expect(formatGuestList([])).to.eql([]);
  });
});
